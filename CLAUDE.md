# CLAUDE.md — دليل المشروع لجلسات Claude

مرجع سريع لأي جلسة تطوير على هذا المستودع. **هذا المستودع هو المكان الوحيد لكل
تحديثات تطبيق «لا تجلس طويلًا».**

## الهوية
- **الاسم/الشعار:** لا تجلس طويلًا
- **applicationId:** `com.alaoufi.health_reminder`
- **النوع:** تطبيق Flutter مستقلّ (أندرويد)، عربيّ RTL، بلا إنترنت.
- **الغرض:** تذكير صحّيّ بكسر الجلوس — تغطية شاشة **صامتة** بخلفيات متغيّرة
  وعبارات تحفيزية وعدّاد، لا تُفتح إلا بانتهاء المدّة أو إدخال رمز معقّد.

## الأصل والسياق
انبثق من ميزة كانت داخل تطبيق «ملاحظات» (`alaoufi/note_only`) ثم فُصل بطلب المالك.
**التفاصيل الكاملة وطلب المالك النصّي وقرارات التصميم:** `docs/السياق_والمتطلبات.md`.
**دليل المطوّر التقنيّ الشامل** (البنية، نموذج الجدولة، الجسر مع native، الأذونات،
CI والتوقيع، المزالق): `docs/دليل_المطوّر.md`.

## البنية
```
lib/
├─ main.dart            # الإقلاع + الثيم + RTL (Asia/Riyadh)
├─ home_screen.dart     # الرئيسية: الحالة، الفترة القادمة، تجربة، إعدادات
├─ settings_screen.dart # ضبط الفترات (بداية/مدّة/تفعيل) + رمز التخطّي
├─ break_screen.dart    # شاشة التغطية الصامتة (خلفيات + عبارات + عدّاد + تخطٍّ)
├─ break_service.dart   # الإعداد ومنطق الفترات (SharedPreferences)
└─ notify_service.dart  # جدولة الإشعارات الصامتة بملء الشاشة
```

## البناء والفحص
```bash
flutter pub get
flutter analyze     # يجب أن يكون بلا أخطاء
flutter test        # اختبارات النموذج
flutter build apk --release
```

## قواعد ثابتة
- **بلا صوت ولا اهتزاز** في أي تنبيه.
- الحفاظ على عربيّة RTL وبساطة الاستخدام.
- **لا تُعِد** إضافة `flutter_timezone` (تكسر البناء بتعارض JVM) — التوقيت مثبّت
  على `Asia/Riyadh`.
- عند إصدار نشر: استخدم مفتاح توقيع خاصًّا **ثابتًا** (لا debug) عبر
  `android/key.properties` (غير متعقَّب)، وارفع `versionCode` في كل إصدار.
- بعد أي تحديث: حدِّث «سجلّ التحديثات» في `docs/السياق_والمتطلبات.md`.
